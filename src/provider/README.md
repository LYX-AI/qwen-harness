# Provider Layer

This folder will contain the model provider abstraction.

The first implementation should target an OpenAI-compatible endpoint so the harness can work with:

- llama.cpp server
- Ollama-compatible adapters
- vLLM
- other local or low-cost endpoints
